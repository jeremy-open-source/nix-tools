#!/usr/bin/env python3

"""
Build YAML file indexes for directories, detect duplicate content, and compare
one index against another.

Design notes:
- `get_file_info()` is the low-level collection step that reads file details and
  stores them in the YAML index with minimal calculation.
- The YAML index is intended to reduce repeated heavy file operations such as
  hashing and metadata extraction.
- Higher-level business logic should be derived from `FileInfo` at read time
  instead of being persisted back into the index when avoidable.
- If a new use case needs more interpretation, add that on top of `FileInfo`
  rather than expanding the stored index with calculated fields.

Common use cases:
- Create an index for an unsorted photo import.
- Create an index for a NAS photo library.
- Refresh an existing index after moving, deleting, and adding files.
- Compare the two indexes to confirm every imported photo exists in the NAS,
  even if the destination folders are different.

Refresh behavior:
- Keep existing index entries when the file path still exists.
- Remove index entries when the file path no longer exists.
- Hash only newly discovered paths.

Examples:
    js-file-index --create-index --directory /photos/import --output-file import.yml
    js-file-index --refresh-index --directory /photos/import --output-file import.yml
    js-file-index --create-index --directory /mnt/nas/photos --output-file nas.yml
    js-file-index --compare-indexes --output-file nas.yml --other-index import.yml
    js-file-index --compare-indexes --output-file nas.yml --other-index import.yml --match-name
"""

import os
import hashlib
import yaml
import argparse
import logging
import mimetypes
import subprocess
import json
import shutil
import re
from datetime import datetime
from typing import Any, NotRequired, TypedDict

logger = logging.getLogger(__name__)

INDEX_VERSION = "2"
DEFAULT_OUTPUT_FILE = "js_file_index.yml"
MEDIA_DATE_FORMATS = (
    "%Y:%m:%d %H:%M:%S%z",
    "%Y:%m:%d %H:%M:%S",
    "%Y-%m-%dT%H:%M:%S.%f%z",
    "%Y-%m-%dT%H:%M:%S%z",
    "%Y-%m-%dT%H:%M:%S.%f",
    "%Y-%m-%dT%H:%M:%S",
    "%Y-%m-%d %H:%M:%S.%f%z",
    "%Y-%m-%d %H:%M:%S%z",
    "%Y-%m-%d %H:%M:%S.%f",
    "%Y-%m-%d %H:%M:%S",
)
PREFERRED_MEDIA_DATE_KEYS = (
    "SubSecDateTimeOriginal",
    "DateTimeOriginal",
    "CreationDate",
    "CreateDate",
    "MediaCreateDate",
    "TrackCreateDate",
    "DateCreated",
    "ModifyDate",
    "MediaModifyDate",
)
FILENAME_DATE_PATTERNS = (
    re.compile(r"(?<!\d)(\d{8})[_-](\d{6})(?!\d)"),
    re.compile(r"(?<!\d)(\d{4})-(\d{2})-(\d{2})[_-](\d{2})-(\d{2})-(\d{2})(?!\d)"),
    re.compile(r"(?<!\d)(\d{8})(?!\d)"),
    re.compile(r"(?<!\d)(\d{4})-(\d{2})-(\d{2})(?!\d)"),
)


class FileInfo(TypedDict):
    file: str
    folder: str
    path: str
    sha256_hash: str
    date_last_modified: str
    date_created: str | None
    date_last_accessed: str
    size: int
    size_human: str
    extension: str | None
    mime_type: str | None
    is_symlink: bool
    exif: NotRequired[dict[str, Any]]


class IndexPayload(TypedDict):
    version: str
    generated_at: str
    contents: list[FileInfo]


class FileMediaDateInfo(TypedDict):
    media_date: str | None
    media_date_source: str | None


class IncorrectDateFolderInfo(TypedDict):
    file: str
    folder: str
    path: str
    media_date: str | None
    media_date_source: str | None
    actual_relative_folder: str | None
    expected_relative_folder: str | None


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(
        description="Manage file indexes with SHA-256 hashes.",
        epilog=(
            "Examples:\n"
            "  js-file-index --create-index --directory /photos/import --output-file import.yml\n"
            "  js-file-index --refresh-index --directory /photos/import --output-file import.yml\n"
            "  js-file-index --create-index --directory /mnt/nas/photos --output-file nas.yml\n"
            "  js-file-index --compare-indexes --output-file nas.yml --other-index import.yml\n"
            "  js-file-index --compare-indexes --output-file nas.yml --other-index import.yml --match-name"
        ),
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    action_group = parser.add_mutually_exclusive_group(required=True)
    action_group.add_argument("--create-index", action="store_true", help="Re-create the file index")
    action_group.add_argument("--add-missing", action="store_true", help="Add missing files to the index")
    action_group.add_argument(
        "--refresh-index",
        action="store_true",
        help="Add new files and remove entries whose files no longer exist",
    )
    action_group.add_argument(
        "--duplicate-hashes",
        action="store_true",
        help="Find duplicate file hashes and print them",
    )
    action_group.add_argument(
        "--duplicate-hashes-prompt-delete",
        action="store_true",
        help="Prompt to delete duplicate files based on hashes",
    )
    action_group.add_argument(
        "--compare-indexes",
        action="store_true",
        help="Check whether every file in --other-index exists in --output-file by hash",
    )
    action_group.add_argument(
        "--identify-incorrect-date-folders",
        action="store_true",
        help="Read the existing index and print files whose folder does not match metadata-derived YYYY/YYYY-mm",
    )
    parser.add_argument(
        "--directory",
        default=os.getcwd(),
        help="Directory to scan (default: current working directory)",
    )
    parser.add_argument(
        "--output-file",
        default=DEFAULT_OUTPUT_FILE,
        help=f"Output YAML file (default: {DEFAULT_OUTPUT_FILE})",
    )
    parser.add_argument(
        "--other-index",
        help="Second YAML index file used with --compare-indexes",
    )
    parser.add_argument(
        "--match-name",
        action="store_true",
        help="With --compare-indexes, require the filename to also exist for matching hashes",
    )
    return parser.parse_args()


def validate_paths(directory_to_scan: str, output_file: str) -> None:
    if not os.path.isdir(directory_to_scan):
        raise ValueError(f"Directory does not exist or is not a directory: {directory_to_scan}")

    if os.path.isdir(output_file):
        raise ValueError(f"Output file points to a directory, not a file: {output_file}")

    output_parent = os.path.dirname(output_file) or "."
    if not os.path.isdir(output_parent):
        raise ValueError(f"Output directory does not exist: {output_parent}")


def validate_existing_file(file_path: str, description: str) -> None:
    if not file_path:
        raise ValueError(f"Missing {description}.")

    if not os.path.isfile(file_path):
        raise ValueError(f"{description} does not exist or is not a file: {file_path}")


def calculate_sha256(file_path: str) -> str:
    sha256_hash = hashlib.sha256()
    with open(file_path, "rb") as f:
        for byte_block in iter(lambda: f.read(1024 * 1024), b""):
            sha256_hash.update(byte_block)
    return sha256_hash.hexdigest()


def human_size(size_bytes: int) -> str:
    units = ["B", "KB", "MB", "GB", "TB", "PB"]
    size = float(size_bytes)

    for unit in units:
        if size < 1024 or unit == units[-1]:
            if unit == "B":
                return f"{int(size)}{unit}"
            if size >= 100:
                return f"{size:.0f}{unit}"
            if size >= 10:
                return f"{size:.1f}{unit}"
            return f"{size:.2f}{unit}"
        size /= 1024.0

    return f"{size_bytes}B"


def get_mime_type(file_path: str) -> str | None:
    mime_type, _ = mimetypes.guess_type(file_path)
    return mime_type


def get_extension(file_path: str) -> str | None:
    _, ext = os.path.splitext(file_path)
    if not ext:
        return None
    return ext.lstrip(".") or None


def get_exif_metadata(file_path: str) -> dict[str, Any]:
    """
    Uses exiftool if available. Returns {} if not available or not applicable.
    """
    if not shutil.which("exiftool"):
        return {}

    try:
        result = subprocess.run(
            ["exiftool", "-j", "-q", "-q", file_path],
            capture_output=True,
            text=True,
            check=True,
        )
        parsed = json.loads(result.stdout)
        if isinstance(parsed, list) and parsed:
            exif_data = parsed[0]
            for key in ["SourceFile", "Directory", "FileName"]:
                exif_data.pop(key, None)
            return exif_data
    except Exception as exc:
        logger.debug("Failed to get exif metadata for %s: %s", file_path, exc)

    return {}


def get_date_created(file_path: str) -> str | None:
    try:
        stat_result = os.stat(file_path)
        birth_time = getattr(stat_result, "st_birthtime", None)
        if birth_time is not None:
            return datetime.fromtimestamp(birth_time).isoformat()
    except Exception:
        pass

    try:
        result = subprocess.run(
            ["stat", "-c", "%w", file_path],
            capture_output=True,
            text=True,
            check=True,
        )
        value = result.stdout.strip()
        if value and value != "-":
            return value
    except Exception:
        pass

    return None



def parse_media_datetime(value: Any) -> datetime | None:
    if not isinstance(value, str):
        return None

    candidate = value.strip()
    if not candidate:
        return None

    if len(candidate) > 6 and candidate[-6] in ["+", "-"] and candidate[-3] == ":":
        compact_offset = candidate[:-6] + candidate[-6:-3] + candidate[-2:]
    else:
        compact_offset = candidate

    for date_format in MEDIA_DATE_FORMATS:
        try:
            return datetime.strptime(compact_offset, date_format)
        except ValueError:
            continue

    return None


def parse_media_datetime_from_filename(file_name: str) -> datetime | None:
    for pattern in FILENAME_DATE_PATTERNS:
        match = pattern.search(file_name)
        if not match:
            continue

        parts = match.groups()
        try:
            if len(parts) == 2:
                return datetime.strptime("".join(parts), "%Y%m%d%H%M%S")
            if len(parts) == 6:
                return datetime.strptime("".join(parts), "%Y%m%d%H%M%S")
            if len(parts) == 1:
                return datetime.strptime(parts[0], "%Y%m%d")
            if len(parts) == 3:
                return datetime.strptime("".join(parts), "%Y%m%d")
        except ValueError:
            continue

    return None


def resolve_media_date(file_info: FileInfo) -> FileMediaDateInfo:
    parsed_filename_date = parse_media_datetime_from_filename(file_info["file"])
    if parsed_filename_date is not None:
        return FileMediaDateInfo(
            media_date=parsed_filename_date.isoformat(),
            media_date_source="filename",
        )

    exif_data = file_info.get("exif", {})
    for key in PREFERRED_MEDIA_DATE_KEYS:
        parsed = parse_media_datetime(exif_data.get(key))
        if parsed is not None:
            return FileMediaDateInfo(
                media_date=parsed.isoformat(),
                media_date_source=f"exif.{key}",
            )

    parsed_created = parse_media_datetime(file_info["date_created"])
    if parsed_created is not None:
        return FileMediaDateInfo(
            media_date=parsed_created.isoformat(),
            media_date_source="filesystem.date_created",
        )

    return FileMediaDateInfo(
        media_date=file_info["date_last_modified"],
        media_date_source="filesystem.date_last_modified",
    )


def build_expected_relative_folder(media_date: str | None) -> str | None:
    if not media_date:
        return None

    parsed = parse_media_datetime(media_date)
    if parsed is None:
        return None

    return f"{parsed:%Y}/{parsed:%Y-%m}"


def actual_relative_folder_suffix(folder_path: str) -> str | None:
    normalized = os.path.normpath(folder_path)
    parts = normalized.split(os.sep)
    if len(parts) < 2:
        return None

    return "/".join(parts[-2:])


def get_file_info(file_path: str) -> FileInfo:
    file_stats = os.stat(file_path)
    abs_path = os.path.abspath(file_path)
    date_created = get_date_created(file_path)
    exif_data = get_exif_metadata(file_path)

    info: FileInfo = {
        "file": os.path.basename(file_path),
        "folder": os.path.dirname(abs_path),
        "path": abs_path,
        "sha256_hash": calculate_sha256(file_path),
        "date_last_modified": datetime.fromtimestamp(file_stats.st_mtime).isoformat(),
        "date_created": date_created,
        "date_last_accessed": datetime.fromtimestamp(file_stats.st_atime).isoformat(),
        "size": file_stats.st_size,
        "size_human": human_size(file_stats.st_size),
        "extension": get_extension(file_path),
        "mime_type": get_mime_type(file_path),
        "is_symlink": os.path.islink(file_path),
    }

    if exif_data:
        info["exif"] = exif_data

    return info


def validate_loaded_index(data: Any) -> IndexPayload:
    if not isinstance(data, dict):
        raise ValueError(
            f"Unsupported index format in YAML. Expected a mapping with keys "
            f"'version', 'generated_at', and 'contents'."
        )

    version = str(data.get("version"))
    if version != INDEX_VERSION:
        raise ValueError(
            f"Unsupported index version: {version!r}. "
            f"Expected version {INDEX_VERSION!r}."
        )

    if "contents" not in data:
        raise ValueError("Invalid index file: missing 'contents' key.")

    if not isinstance(data["contents"], list):
        raise ValueError("Invalid index file: 'contents' must be a list.")

    if "generated_at" not in data:
        raise ValueError("Invalid index file: missing 'generated_at' key.")

    return data


def load_existing_index(file_path: str) -> IndexPayload:
    if not os.path.exists(file_path):
        return {
            "version": INDEX_VERSION,
            "generated_at": datetime.now().isoformat(),
            "contents": [],
        }

    with open(file_path, "r", encoding="utf-8") as f:
        raw = yaml.safe_load(f)

    if raw is None:
        raise ValueError(f"Index file {file_path} is empty.")

    return validate_loaded_index(raw)


def generate_hash_for_directory(directory_path: str, output_yaml_file: str) -> list[FileInfo]:
    files_data: list[FileInfo] = []

    output_abs = os.path.abspath(output_yaml_file)

    for root, dirs, files in os.walk(directory_path):
        dirs.sort()
        files.sort()

        for file in files:
            file_path = os.path.join(root, file)
            abs_file_path = os.path.abspath(file_path)

            if abs_file_path == output_abs:
                logger.debug("Skipping output file %s", file_path)
                continue

            if not os.path.isfile(file_path):
                continue

            logger.info("Looking at file %s", file_path)
            file_info = get_file_info(file_path)
            files_data.append(file_info)

    return files_data


def build_index_payload(contents: list[FileInfo]) -> IndexPayload:
    return {
        "version": INDEX_VERSION,
        "generated_at": datetime.now().isoformat(),
        "contents": contents,
    }


def create_index(directory_to_scan: str, output_yaml_file: str) -> None:
    files_data = generate_hash_for_directory(directory_to_scan, output_yaml_file)
    payload = build_index_payload(files_data)

    with open(output_yaml_file, "w", encoding="utf-8") as yaml_file:
        yaml.safe_dump(payload, yaml_file, default_flow_style=False, sort_keys=False, allow_unicode=True)

    logger.info("File index created and saved in %s", output_yaml_file)


def add_missing(directory_to_scan: str, output_yaml_file: str) -> None:
    existing_index = load_existing_index(output_yaml_file)
    existing_contents = existing_index["contents"]

    # Use path as the unique key rather than just filename
    existing_files = {file_info["path"]: file_info for file_info in existing_contents}

    new_files_data: list[FileInfo] = []

    output_abs = os.path.abspath(output_yaml_file)

    for root, dirs, files in os.walk(directory_to_scan):
        dirs.sort()
        files.sort()

        for file in files:
            file_path = os.path.join(root, file)
            abs_file_path = os.path.abspath(file_path)

            if abs_file_path == output_abs:
                continue

            if abs_file_path not in existing_files:
                logger.info("Adding missing file %s", abs_file_path)
                file_info = get_file_info(file_path)
                new_files_data.append(file_info)

    if new_files_data:
        updated_data = existing_contents + new_files_data
        updated_payload = build_index_payload(updated_data)

        with open(output_yaml_file, "w", encoding="utf-8") as yaml_file:
            yaml.safe_dump(updated_payload, yaml_file, default_flow_style=False, sort_keys=False, allow_unicode=True)

        logger.info("Missing files added to index and saved in %s", output_yaml_file)
    else:
        logger.info("No missing files found to add.")


def iter_directory_file_paths(directory_to_scan: str, output_yaml_file: str) -> set[str]:
    current_paths: set[str] = set()
    output_abs = os.path.abspath(output_yaml_file)

    for root, dirs, files in os.walk(directory_to_scan):
        dirs.sort()
        files.sort()

        for file in files:
            file_path = os.path.join(root, file)
            abs_file_path = os.path.abspath(file_path)

            if abs_file_path == output_abs:
                continue

            if not os.path.isfile(file_path):
                continue

            current_paths.add(abs_file_path)

    return current_paths


def refresh_index(directory_to_scan: str, output_yaml_file: str) -> None:
    existing_index = load_existing_index(output_yaml_file)
    existing_contents = existing_index["contents"]
    existing_by_path = {file_info["path"]: file_info for file_info in existing_contents}

    current_paths = iter_directory_file_paths(directory_to_scan, output_yaml_file)

    kept_contents: list[FileInfo] = []
    removed_count = 0
    for path, file_info in existing_by_path.items():
        if path in current_paths:
            kept_contents.append(file_info)
        else:
            removed_count += 1

    new_files_data: list[FileInfo] = []
    for path in sorted(current_paths):
        if path in existing_by_path:
            continue

        logger.info("Adding new file %s", path)
        new_files_data.append(get_file_info(path))

    refreshed_contents = kept_contents + new_files_data
    refreshed_payload = build_index_payload(refreshed_contents)

    with open(output_yaml_file, "w", encoding="utf-8") as yaml_file:
        yaml.safe_dump(refreshed_payload, yaml_file, default_flow_style=False, sort_keys=False, allow_unicode=True)

    logger.info(
        "Index refreshed in %s. Kept %s existing entries, added %s new files, removed %s missing entries.",
        output_yaml_file,
        len(kept_contents),
        len(new_files_data),
        removed_count,
    )


def find_duplicate_hashes(output_yaml_file: str) -> dict[str, list[str]]:
    existing_index = load_existing_index(output_yaml_file)
    existing_contents = existing_index["contents"]

    hash_map: dict[str, list[str]] = {}
    for file_info in existing_contents:
        sha256_hash = file_info["sha256_hash"]
        path = file_info["path"]

        if sha256_hash in hash_map:
            hash_map[sha256_hash].append(path)
        else:
            hash_map[sha256_hash] = [path]

    duplicates = {sha256_hash: paths for sha256_hash, paths in hash_map.items() if len(paths) > 1}
    return duplicates


def print_duplicate_hashes(duplicates: dict[str, list[str]]) -> None:
    if not duplicates:
        print("No duplicate hashes found.")
        return

    for sha256_hash, paths in duplicates.items():
        print(f"Hash: {sha256_hash}")
        for path in paths:
            print(f" -> {path}")


def prompt_delete_duplicates(duplicates: dict[str, list[str]]) -> None:
    if not duplicates:
        print("No duplicate hashes found.")
        return

    for sha256_hash, paths in duplicates.items():
        print(f"\nDuplicate files for hash: {sha256_hash}")
        mutable_paths = list(paths)

        while len(mutable_paths) > 1:
            for i, path in enumerate(mutable_paths, 1):
                print(f"[{i}] {path}")

            try:
                to_delete = input("Choose a file number to delete or 'skip' to keep the rest: ")
                if to_delete.lower() == "skip":
                    break

                index = int(to_delete) - 1
                if 0 <= index < len(mutable_paths):
                    file_to_delete = mutable_paths.pop(index)
                    os.remove(file_to_delete)
                    print(f"Deleted: {file_to_delete}")
                else:
                    print("Invalid selection. Please try again.")
            except (ValueError, IndexError):
                print("Invalid input. Please enter a number corresponding to the file or 'skip'.")
            except OSError as exc:
                print(f"Failed to delete file: {exc}")


def build_hash_name_lookup(index_contents: list[FileInfo]) -> dict[str, set[str]]:
    lookup: dict[str, set[str]] = {}
    for file_info in index_contents:
        sha256_hash = file_info["sha256_hash"]
        file_name = file_info["file"]
        lookup.setdefault(sha256_hash, set()).add(file_name)
    return lookup


def compare_indexes(output_yaml_file: str, other_index_file: str, match_name: bool) -> bool:
    existing_index = load_existing_index(output_yaml_file)
    other_index = load_existing_index(other_index_file)

    existing_lookup = build_hash_name_lookup(existing_index["contents"])
    missing_hashes: list[dict[str, str]] = []
    name_mismatches: list[dict[str, str]] = []
    matched_files = 0

    for file_info in other_index["contents"]:
        sha256_hash = file_info["sha256_hash"]
        file_name = file_info["file"]
        path = file_info.get("path") or os.path.join(file_info["folder"], file_info["file"])
        existing_names = existing_lookup.get(sha256_hash)

        if not existing_names:
            missing_hashes.append(
                {
                    "path": path,
                    "file": file_name,
                    "sha256_hash": sha256_hash,
                }
            )
            continue

        if match_name and file_name not in existing_names:
            name_mismatches.append(
                {
                    "path": path,
                    "file": file_name,
                    "sha256_hash": sha256_hash,
                }
            )
            continue

        matched_files += 1

    print(f"Compared {len(other_index['contents'])} files from {other_index_file}")
    print(f"Against {len(existing_index['contents'])} files in {output_yaml_file}")
    print(f"Matched: {matched_files}")
    print(f"Missing by hash: {len(missing_hashes)}")

    if match_name:
        print(f"Hash matched but filename missing: {len(name_mismatches)}")

    if missing_hashes:
        print("\nMissing hashes:")
        for item in missing_hashes:
            print(f" - {item['path']} [{item['sha256_hash']}]")

    if match_name and name_mismatches:
        print("\nHash matched but filename not found:")
        for item in name_mismatches:
            print(f" - {item['path']} [{item['sha256_hash']}]")

    return not missing_hashes and (not match_name or not name_mismatches)


def print_incorrect_date_folder(file_info: IncorrectDateFolderInfo) -> None:
    print(f"\n- file: {file_info['file']}")
    print(f"  folder: {file_info['folder']}")
    print(f"  path: {file_info['path']}")
    print(f"  media_date: {file_info['media_date']}")
    print(f"  media_date_source: {file_info['media_date_source']}")
    print(f"  actual_relative_folder: {file_info['actual_relative_folder']}")
    print(f"  expected_relative_folder: {file_info['expected_relative_folder']}")


def identify_incorrect_date_folders(output_yaml_file: str) -> int:
    incorrect_count = 0
    existing_index = load_existing_index(output_yaml_file)

    for file_info in existing_index["contents"]:
        media_date_info = resolve_media_date(file_info)
        expected_relative_folder = build_expected_relative_folder(media_date_info["media_date"])
        actual_relative_folder = actual_relative_folder_suffix(file_info["folder"])

        if expected_relative_folder is None:
            path = file_info["path"]
            logger.warning("Skipping %s because no metadata date could be derived from the index", path)
            continue

        if actual_relative_folder != expected_relative_folder:
            enriched_file_info: IncorrectDateFolderInfo = {
                "file": file_info["file"],
                "folder": file_info["folder"],
                "path": file_info["path"],
                "media_date": media_date_info["media_date"],
                "media_date_source": media_date_info["media_date_source"],
                "expected_relative_folder": expected_relative_folder,
                "actual_relative_folder": actual_relative_folder,
            }
            if incorrect_count == 0:
                print("Files in the wrong YYYY/YYYY-mm folder:")
            print_incorrect_date_folder(enriched_file_info)
            incorrect_count += 1

    if incorrect_count == 0:
        print("All scanned files are in the correct YYYY/YYYY-mm folder.")
    else:
        print(f"\nTotal incorrect date folders: {incorrect_count}")

    return incorrect_count


def main() -> None:
    args = parse_args()

    loglevel = os.getenv("LOGLEVEL", "INFO").upper()
    logging.basicConfig(level=getattr(logging, loglevel, logging.INFO))

    directory_to_scan = os.path.abspath(args.directory)
    output_file = os.path.abspath(args.output_file)

    try:
        if args.create_index:
            validate_paths(directory_to_scan, output_file)
            create_index(directory_to_scan, output_file)
        elif args.add_missing:
            validate_paths(directory_to_scan, output_file)
            add_missing(directory_to_scan, output_file)
        elif args.refresh_index:
            validate_paths(directory_to_scan, output_file)
            refresh_index(directory_to_scan, output_file)
        elif args.duplicate_hashes:
            validate_existing_file(output_file, "Index file")
            duplicates = find_duplicate_hashes(output_file)
            print_duplicate_hashes(duplicates)
        elif args.duplicate_hashes_prompt_delete:
            validate_existing_file(output_file, "Index file")
            duplicates = find_duplicate_hashes(output_file)
            prompt_delete_duplicates(duplicates)
        elif args.compare_indexes:
            other_index_file = os.path.abspath(args.other_index) if args.other_index else None
            validate_existing_file(output_file, "Primary index file")
            validate_existing_file(other_index_file, "Comparison index file")
            success = compare_indexes(output_file, other_index_file, args.match_name)
            if not success:
                raise SystemExit(1)
        elif args.identify_incorrect_date_folders:
            validate_existing_file(output_file, "Index file")
            incorrect_count = identify_incorrect_date_folders(output_file)
            if incorrect_count:
                raise SystemExit(1)
    except ValueError as exc:
        logger.error(str(exc))
        raise SystemExit(1)


if __name__ == "__main__":
    main()
