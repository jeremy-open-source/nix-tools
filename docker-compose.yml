version: '3.6'
services:
  mermaidjs:
    build:
      context: build/mermaid-js
#  php-tools:
#    build:
#      context: .
#      dockerfile: build/php-tools/Dockerfile
#      args:
#        FROM_IMAGE: "ubuntu:${PHP_TOOLS_UBUNTU_VERSION}"
#    volumes:
#      - "/etc/passwd:/etc/passwd:ro"
#      - "/etc/group:/etc/group:ro"
#      - "/tmp:/tmp"
  yarn:
    build:
      context: .
      dockerfile: build/yarn/Dockerfile
      args:
        FROM_IMAGE: "ubuntu:${YARN_UBUNTU_VERSION}"
